"""居民：角色定义（``agent.py``）与内部状态（``state.py``）。

⚠️ **这个文件要保持空的**。它一旦 ``from agents.agent import ...``，
任何人想拿 ``agents.state`` 里一个小函数，都会连带把 LLM 客户端、
记忆系统和整个工具注册表一起导进来——包的 ``__init__`` 决定了这个包
的最小导入代价。
"""
