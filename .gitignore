# Secrets and local environment
.env
.env.*
!.env.example
backend/.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Node
node_modules/
npm-debug.log*

# Runtime output
*.log
backend/logs/
backend/simulation_progress.json
backend/conversations.json
backend/memory_data.json
backend/agent_internal_states/*.json
backend/memory/agent_memory_banks/*.json
backend/memory/agent_personas/
backend/daily_plans.backup-*.json

# Local diagnostics and working documents
diagnostic_*
ui-panel-screenshot.png
verification-screenshot.png
notes.docx
frontend/assets/*666.png
frontend/assets/park1.png

# Editor and operating-system files
.DS_Store
Thumbs.db
.idea/
.vscode/

# 邮件数据属于运行期状态，不进版本库
backend/mailboxes.json
backend/inventory.json    # 改名成 economy 之前的旧文件
backend/economy.json
backend/goals.json

# 归档的开发期产物（调试图、截图、日志），确认后可整个删掉
_archive/

# 原子写（临时文件 + 替换）在进程被杀时留下的孤儿。正主是同名的 .json，
# 这些只是半个文件，绝不该进版本库。
*.tmp
